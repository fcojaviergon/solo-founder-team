# CLAUDE.md — [NOMBRE_PROYECTO]

## Proyecto
[NOMBRE_PROYECTO] — [Descripción en una línea]
Stack: [ej: Next.js 15 + Supabase + Tailwind + Vercel]
Solo founder: optimizar para velocidad de shipping con calidad mínima viable.

## Principios
- Ship small, ship often. PRs < 300 líneas.
- Tests para lógica de negocio crítica. No 100% coverage.
- Conventional commits siempre (feat/fix/chore/docs/refactor).
- Si algo puede fallar en producción, necesita error handling.
- Cada error de Claude se convierte en una regla aquí abajo.

## Arquitectura
Ver docs/architecture.md para decisiones completas.
- [COMPLETAR: patrón arquitectónico]
- [COMPLETAR: capas principales]

## Convenciones de Código
- TypeScript strict mode
- Nombres en inglés, comentarios en español cuando añadan valor
- Componentes: PascalCase. Hooks: use + camelCase
- Archivos: kebab-case.tsx

## Herramientas
- **Linter/Formatter**: Biome (NO usar Prettier ni ESLint)
- **Ejecutar lint**: `npx @biomejs/biome check --write ./src`
- **Build**: `npm run build`
- **Tests**: `npm run test`
- **Type check**: `npx tsc --noEmit`

## Skills del Proyecto
Este proyecto tiene skills custom que definen convenciones específicas:
- Frontend: .claude/skills/[TU-SKILL-FRONTEND]/SKILL.md
- Backend: .claude/skills/[TU-SKILL-BACKEND]/SKILL.md
Siempre consultar estos skills antes de implementar nuevo código.

## Workflow
Spec (solo si complejo) → Plan → Implement → Test → Review → Ship
Skills globales disponibles en ~/.claude/skills/ para cada fase.
Usar /write-spec solo para features de 10+ archivos o integraciones.
Usar subagents para tareas pesadas que contaminen contexto.

## Contexto Actual
Plan activo: docs/active-plan.md
Specs: specs/ (solo features complejas)

## Reglas Aprendidas
<!-- Agregar reglas aquí cada vez que Claude cometa un error repetido -->
<!-- Ejemplo: "No usar fetch directo, siempre usar el wrapper api.ts" -->
